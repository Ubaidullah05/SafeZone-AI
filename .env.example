# Frontend (Vite) - copy to frontend/.env
VITE_API_BASE_URL=http://127.0.0.1:8000

# Backend (future use - not required to run the MVP)
# DATABASE_URL=postgresql://user:password@localhost:5432/safezone_ai
# MAP_TILE_PROVIDER=osm
